<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- background color-->
        <item name="android:windowBackground">@color/white</item>
    </style>

    <!-- 透明主题-->
    <style name="AppTheme.Translate" parent="AppTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Dialog主题-->
    <style name="AppDialogTheme" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimAmount">0.6</item>
    </style>

    <style name="LoadingDialogTheme" parent="@style/AppDialogTheme">
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="BaseBottomInOutDialog" parent="@style/AppDialogTheme">
        <item name="android:windowAnimationStyle">@style/dialog_bottom_in_out_anim_style</item>
    </style>

    <style name="dialog_bottom_in_out_anim_style">
        <item name="android:windowEnterAnimation">@anim/dialog_open_anim</item>
        <item name="android:windowExitAnimation">@anim/dialog_close_anim</item>
    </style>
</resources>